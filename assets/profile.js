document.querySelectorAll('.credential').forEach(button=>{button.addEventListener('click',()=>{const active=button.classList.contains('pinned');document.querySelectorAll('.credential').forEach(b=>b.classList.remove('pinned'));button.classList.toggle('pinned',!active)});button.addEventListener('keydown',e=>{if(e.key==='Escape'){button.classList.remove('pinned');button.blur()}})});document.addEventListener('click',e=>{if(!e.target.closest('.credential'))document.querySelectorAll('.credential').forEach(b=>b.classList.remove('pinned'))});